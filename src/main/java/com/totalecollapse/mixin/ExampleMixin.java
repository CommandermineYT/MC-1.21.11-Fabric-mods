package com.totalecollapse.mixin;

// This mixin needs to be moved to src/client/java/ since it targets Minecraft class (client-side only)
// For now, we'll remove it from the mixin config and keep just an empty file

public class ExampleMixin {
    // Empty - will be removed or replaced with server-side mixin if needed

}